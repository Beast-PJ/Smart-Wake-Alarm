<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartWake" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SmartWake" parent="Base.Theme.SmartWake" />
    <!-- Chip Style -->
    <style name="ChipStyle">
    <item name="chipBackgroundColor">@color/chipBackground</item>
    <item name="chipStrokeColor">@color/chipStroke</item>
    <item name="chipStrokeWidth">1dp</item>
    </style>
</resources>